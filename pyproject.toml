[build-system]
requires = ["flit_core >=3.2,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "lumache"
authors = [{name = "IgnitionGroup", email = "Mpilo.Gumede@IgnitionGroup.co.za"}]
dynamic = ["version", "description"]
